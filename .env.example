# Caddy listen target. Use ":80" for plain HTTP, or "your.domain.com" for HTTPS.
APP_DOMAIN=:80

# Host port mapping for the web service.
HTTP_PORT=80
HTTPS_PORT=443

# PostgreSQL password used by db + app + migration container.
# deploy-xs.sh auto-generates this if it is empty or "change_me".
POSTGRES_PASSWORD=change_me

# Server runtime options.
AUTH_SESSION_TTL=720h
AUDIT_RECENT_LIMIT_X=200
AUDIT_SAVED_LIMIT_Y=50
